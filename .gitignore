# SQLite database files
*.db
*.db-journal
*.db-wal
*.db-shm

# Local data exports
/data/*.sqlite
/site/data/*.sqlite
# Frontend runtime config (copy from site/app-config.example.js)
# Keep project-wide default checked in so deployments work out of the box.
# Node dependencies
/node_modules/
