# Copy this file to `.env` and tweak the values for your environment.
# Every variable is optional unless marked as required.
#
# After configuring, validate your setup with: npm run validate:env

# Port for the Express API (defaults to 8787)
PORT=8787

# Absolute or relative path to the JSON datastore (defaults to api/state.json)
# DATA_PATH=/absolute/path/to/state.json

# Optional override for the comments JSON file (defaults to alongside DATA_PATH)
# COMMENTS_DATA_PATH=/absolute/path/to/comments.json

# Optional override for the character drafts JSON file (defaults next to DATA_PATH)
# CHARACTER_DRAFTS_PATH=/absolute/path/to/character_drafts.json

# --- Supabase configuration ---
# Base URL for your Supabase project (required when using Supabase)
# SUPABASE_URL=https://your-project.supabase.co

# Service role key with read/write access (required when using Supabase)
# SUPABASE_SERVICE_ROLE_KEY=service-role-key

# Single-row table that stores the JSON blob (defaults to oracle_state)
# SUPABASE_TABLE=oracle_state

# Row id within SUPABASE_TABLE that stores the JSON blob (defaults to shared)
# SUPABASE_ROW_ID=shared

# Storage driver: `json` (single row) or `tables` (normalized schema, defaults to json)
# SUPABASE_STORAGE_MODE=json

# Name of the character drafts table used when SUPABASE_STORAGE_MODE=json (defaults to character_drafts)
# SUPABASE_CHARACTER_DRAFTS_TABLE=character_drafts

# Normalized table names (used when SUPABASE_STORAGE_MODE=tables)
# SUPABASE_SESSIONS_TABLE=sessions
# SUPABASE_SESSION_PLAYERS_TABLE=session_players
# SUPABASE_ROSTER_EXTRAS_TABLE=roster_extras
# SUPABASE_ROSTER_META_TABLE=roster_meta
# SUPABASE_AVAILABILITY_TABLE=availability
# SUPABASE_BUILD_CARDS_TABLE=build_cards
# SUPABASE_COMMENTS_TABLE=comments
# SUPABASE_PLAYER_ACCESS_TABLE=player_access

# Optional token that grants admin rights to the /player_access endpoints
# PLAYER_ACCESS_ADMIN_TOKEN=super-secret-token
